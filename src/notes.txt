jsonwebtoken — для генерации и валидации токенов.
bcryptjs — для хеширования паролей.
cors — для разрешения запросов с другого домена (между клиентом и сервером).
body-parser — для обработки данных запроса.